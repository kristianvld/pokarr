services:
  pokarr:
    image: ghcr.io/kristianvld/pokarr:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      TZ: Europe/Amsterdam
    volumes:
      - ./data:/app/data
